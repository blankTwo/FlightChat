# Flight Chat 项目交接文档

> 更新日期：2026-08-15  
> 项目版本：`0.1.1`  
> 技术栈：Tauri v2 + Rust + React + TypeScript + Vite

## 1. 项目目的

Flight Chat 是一个轻量桌面聊天界面，不直接替代 ChatGPT 网页，而是把已经登录的 `chatgpt.com` 网页作为后台真实会话载体：

```text
用户在 Flight 输入
  → 同步到已登录的 ChatGPT WebView
  → 由网页执行 @、/、图片、发送与模型回复
  → Flight 监听网页的回复流
  → 在本地轻量 Markdown 聊天界面展示
```

这样做的核心目的：

- 避开长对话时 ChatGPT 网页的大量 DOM、资源加载和卡顿；
- 保留网页登录态、已有网页历史会话；
- 保留网页端的 `@` 插件/应用选择与 `/` 命令能力；
- 在 Flight 中获得固定顶部、固定输入框、可滚动消息区以及 Markdown/代码渲染。

**边界：** 当前方案依赖 ChatGPT 网页的页面结构及内部接口，是网页桥接方案；不是官方 OpenAI API 客户端。

## 2. 当前架构

### 窗口与职责

| 窗口 | 作用 |
| --- | --- |
| `main` | Flight 主界面，负责对话渲染、输入、会话列表和状态提示。 |
| `chatgpt-session` | 已登录的 ChatGPT 网页 WebView。平时隐藏；需要登录、网页历史或分屏预览时显示。 |
| `flight-quick` | 原生悬浮小窗。网页长会话仍在骨架屏加载时，用户仍可点击进入 Flight。 |

### 数据流

```text
React (src/App.tsx)
  │ invoke / listen
  ▼
Rust (src-tauri/src/main.rs)
  │ WebView.eval(CHATGPT_BRIDGE_SCRIPT)
  ▼
ChatGPT WebView
  │ 拦截 fetch、读取 SSE、监听 DOM、调用 Tauri command
  ▼
relay_stream_event
  │ emit_to("main", "flight://stream", payload)
  ▼
React 增量更新消息、状态和会话列表
```

### 关键网页接口（当前观察到）

| 用途 | 网页接口/路径 | 当前处理方式 |
| --- | --- | --- |
| 历史标题列表 | `/backend-api/conversations?offset=0&limit=28&order=updated&is_archived=false&is_starred=false` | 读取、解析并在 Flight 会话列表展示。 |
| 指定会话详情 | `/backend-api/conversation/{id}` | 解析 `mapping`，按时间排序后回显最近消息。 |
| 模型流式回复 | `/backend-api/f/conversation`、`/backend-api/conversation/{id}/resume` | 拦截 `fetch` 的 Response，解析 SSE 后转发增量。 |

这些路径是网页内部实现，可能随网页改版而改变。

## 3. 已实现功能

### 3.1 登录与 Cookie

- 首次启动会在后台创建隐藏的 `chatgpt-session` WebView。
- 能正常打开 ChatGPT 登录页；检测到网页输入框后向主界面发送 `session_ready`。
- 可从 Cookie-Editor 导出的 JSON 导入 Cookie，仅接收 `chatgpt.com` / `openai.com` 域。
- 可把当前 Cookie 导出为 Cookie-Editor 兼容 JSON，并直接复制到剪贴板。
- 网页右下角已注入“进入飞行模式”“导入 Cookie”“导出 Cookie”“精简页面”按钮。

### 3.2 Flight 聊天界面

- 顶部区域固定，包含会话标题、状态、历史入口、分屏入口等操作。
- 中间消息区独立滚动。
- 底部输入区固定。
- 支持 Markdown、GFM、代码块和代码复制。
- 支持粘贴图片并随消息发送至网页。
- 支持新建会话、发送消息、接收增量回复、会话标题更新和错误状态显示。

### 3.3 网页回复桥接

- 注入脚本重写当前网页的 `window.fetch`。
- 捕获 SSE 中的增量文本、会话 ID、标题生成、完成和错误事件。
- 通过 Rust command `relay_stream_event` 转给主窗口事件 `flight://stream`。
- 如果网络流没有成功捕获，使用 DOM `MutationObserver` 作为回复文本回退通道。
- 已针对 `/conversation/{id}/resume` 进行探针捕获。

### 3.4 `@` 与 `/` 命令桥接

- Flight 输入变化会同步到网页真实输入框；删除也会同步。
- 输入 `@` 或 `/` 后，读取网页候选浮层并在 Flight 显示候选列表。
- 在 Flight 选择候选时，会向网页派发指针/鼠标事件，尽量触发网页原本的标签选择逻辑。
- 已处理选择后追加文本、标签删除和草稿同步等基本流程。
- 支持双屏预览：同时查看 Flight 和网页，可用于确认 `@` / `/` 是否已正确进入网页。

### 3.5 网页历史与长会话优化

- Flight 可读取 ChatGPT 网页历史会话标题并支持搜索、选择。
- 点击历史标题后：先请求指定会话详情、立即把最新消息交给 Flight，再让隐藏网页在后台切到该会话。
- Flight 的历史回显数量可选：`2 / 5 / 10 / 20 / 50` 条。
- 为避免长会话拖垮网页：网页端会删除较早的消息根节点，仅保留最新两条。刷新网页可恢复完整网页 DOM。
- 已加入最近会话详情缓存与并发请求去重，避免进入 Flight 时重复请求同一份大历史。
- 切换长会话、关闭分屏、新建会话等路径已加入状态解除和延迟重显逻辑，降低窗口竞争导致的卡住概率。

### 3.6 构建与安装包

- 已配置 Windows NSIS 与 MSI 打包。
- 已生成过：
  - `src-tauri/target/release/bundle/nsis/Flight Chat_0.1.1_x64-setup.exe`
  - `src-tauri/target/release/bundle/msi/Flight Chat_0.1.1_x64_en-US.msi`
- 应优先以 release 可执行文件进行本机验证，而不是只检查安装包能否构建。

## 4. 未实现或尚未完全验证的内容

| 项目 | 当前状态 | 后续建议 |
| --- | --- | --- |
| 官方 API 替代主链路 | 不做 | 官方 API 无法读取 ChatGPT 网页历史，也不能直接使用网页 `@` 插件；不符合本项目核心目标。 |
| 全量/分页网页历史列表 | 当前只请求 28 或 100 条 | 若需要更多历史，补充分页加载与“加载更多”。 |
| 网页改版适配 | 持续风险 | 将选择器、SSE 解析、接口路径抽成独立适配层，并添加诊断日志。 |
| `@` / `/` 的所有候选类型 | 已覆盖基本选择流程 | 不同插件、富卡片或新版浮层可能有不同 DOM；需按实际失败案例补选择器和事件。 |
| 图片/附件的所有形式 | 已支持粘贴图片 | 文件拖拽、多文件、网页特有附件卡片尚未形成完整回归测试。 |
| 自动化回归测试 | 未建立 | 建议至少覆盖：登录态、普通消息、`@` 选择、历史切换、长会话、分屏、Cookie 导入导出。 |
| 本地会话存储 | 未做 | 现在历史来源是网页；如将来增加官方 API 模式，需要 SQLite 保存 API 会话 ID、标题和更新时间。 |
| 多账号管理 | 未做 | 当前 Cookie/登录态对应一个 WebView 数据目录。 |

## 5. 已知限制与处理原则

1. **网页资源加载慢时，网页本身仍可能卡顿。** Flight 已尽量提前请求会话详情并允许从悬浮入口进入，但无法控制 chatgpt.com 加载其动态资源。
2. **网页 DOM 精简是性能优化，不是数据删除。** 它只删除当前 WebView 的已渲染节点；刷新页面会重新加载完整网页会话。
3. **网页接口不能当作稳定公开 API。** 任何 `backend-api` 变化都可能导致会话列表、历史回显或流式监听失效。
4. **核心验收优先级是 `@` 插件。** 后续改动不能为了“纯 API 化”而破坏网页输入框、候选浮层和标签选择同步。
5. **Cookie 导入/导出需要保持兼容 Cookie-Editor JSON 数组格式。** 不要把 Cookie 文本写入日志或提交到仓库。

## 6. 关键文件

| 文件 | 职责 |
| --- | --- |
| `src/App.tsx` | Flight React UI、状态管理、`flight://stream` 事件消费、会话列表、输入、Markdown 渲染。 |
| `src-tauri/src/main.rs` | Tauri commands、窗口控制、Cookie 导入导出、桥接脚本、网页交互。 |
| `src-tauri/build.rs` | 自定义 command 的 ACL 生成配置。 |
| `src-tauri/capabilities/default.json` | 主窗口所允许调用的 command。 |
| `src-tauri/capabilities/chatgpt-bridge.json` | ChatGPT WebView 桥接脚本所允许调用的 command。 |
| `src-tauri/tauri.conf.json` | 应用窗口、版本、构建和打包配置。 |
| `package.json` | 前端依赖和 npm 脚本。 |

## 7. Rust Commands 与事件

### 前端可调用 commands

```text
import_cookies
export_cookies
enter_flight_mode
prepare_web_bridge
exit_flight_mode
new_conversation
load_conversation_list
select_conversation
send_message
update_command_draft
select_command_option
update_command_suffix
clear_command_selection
set_split_view
```

### 事件

主界面监听：

```text
flight://stream
```

常见事件类型：

```text
session_ready
session_login_required
bridge_ready
enter_flight
conversation_loading
conversation
conversation_list
history
assistant_start
delta
complete
title
command_suggestions
error
probe
```

新增 Rust command 时必须同时更新以下位置，否则 WebView 会出现 `not allowed`：

1. `src-tauri/src/main.rs` 的 `invoke_handler`；
2. `src-tauri/build.rs`；
3. `src-tauri/capabilities/default.json`；
4. 如果由网页桥接脚本调用，还要更新 `src-tauri/capabilities/chatgpt-bridge.json`。

`src-tauri/gen/` 与 `src-tauri/permissions/autogenerated/` 为生成物，不应手工维护。

## 8. 本地运行、验证与打包

### 前端开发

```powershell
npm run dev
```

### Tauri 开发模式

```powershell
npm run tauri dev
```

### 推荐的 release 验证流程

```powershell
npm run build
cd src-tauri
cargo build --release --features custom-protocol
Start-Process .\target\release\flight-chat.exe
```

### 构建安装包

```powershell
npm run tauri build
```

## 9. 每次改动后的最小验收清单

1. 启动 release 程序；未登录时可打开登录页，已登录时能自动进入 Flight。
2. 新建对话，发送普通文本，确认 Flight 收到增量与完成状态。
3. 输入 `@` 或 `/`，确认候选出现；选择一个候选后，网页与 Flight 都保持一致。
4. 粘贴一张图片并发送。
5. 打开历史列表，选择短会话与长会话各一次；确认只回显所选条数。
6. 打开、关闭分屏；关闭后确认 Flight 输入仍可用。
7. 导出 Cookie 到剪贴板，并用导入流程检查 JSON 格式。
8. 网页长会话下使用“精简页面”，确认网页可恢复交互；刷新确认原网页历史仍存在。

## 10. 后续开发优先级

1. 给网页桥接补充可导出的诊断日志（接口命中、事件类型、选择器是否找到、异常原因）。
2. 对 `@` / `/` 的不同浮层类型做针对性兼容，而非改成官方 API。
3. 完成历史列表分页和加载更多。
4. 把桥接脚本拆成更小的模块或模板，降低 `main.rs` 中超长 JavaScript 的维护难度。
5. 建立最小回归测试和每次 release 的手工验收记录。

